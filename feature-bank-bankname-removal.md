# 方案一结算账户信息验证去除开户行方案

| 项目 | 内容 |
|---|---|
| 版本 | v1.3 |
| 更新日期 | 2026-08-14 |
| 状态 | 已完成（原型已更新并上线） |
| 作者 | 产品经理 |
| 所属需求 | 一级机构手机号找回方案（PRD v1.3） |

---

## 1. 背景与决策

经评审确认，**最终方案采用方案一（双验证方式）**。方案一中「结算账户信息验证」环节原包含 4 个字段：机构ID、开户名称、开户账号、开户行。

业务上确认**开户行不做校验**，理由：
- 开户行信息非业务核心校验项，且不同银行分支机构名称复杂，用户输入成本高、易出错；
- 机构ID + 开户名称 + 开户账号已足以定位并校验结算账户，开户行校验价值低。

## 2. 方案目标

- 去除方案一中「结算账户信息验证」的**开户行**输入框；
- 不再校验开户行字段；
- 方案二（直达结算账户信息验证）**保持原样**（仍含开户行），作为备选方案留存。

## 3. 变更内容

### 3.1 表单字段变更

```
结算账户信息验证页（方案一，变更后）
  ├─ 机构ID        （必填，保留）
  ├─ 开户名称      （必填，保留）
  ├─ 开户账号      （必填，保留）
  └─ 开户行        （❌ 已移除，不再校验）
```

### 3.2 交互变更

| 操作 | 变更前 | 变更后 |
|---|---|---|
| 开户行输入框 | 显示，必填 | **移除** |
| 点击「下一步」校验 | 校验 4 项，开户行为空弹「请输入开户行」 | 仅校验 3 项 |
| 校验通过 | 跳转换绑手机号页 | 不变 |

## 4. 接口变更

```
POST /api/org/verify-bank-account
```

**请求参数（变更后）**
| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| orgId | string | 是 | 机构ID |
| accountName | string | 是 | 开户名称 |
| accountNo | string | 是 | 开户账号 |

> `bankName`（开户行）字段已从请求参数中移除。

**返回示例**
```json
{
  "code": 0,
  "data": { "verified": true }
}
```

## 5. 方案差异对照

| 方案 | 结算账户信息验证字段 | 开户行 |
|---|---|---|
| **方案一**（双验证方式选择，最终采用） | 机构ID、开户名称、开户账号 | ❌ 已去除 |
| 方案二（直达结算账户验证，备选） | 机构ID、开户名称、开户账号、开户行 | ✅ 保留 |

## 6. 涉及原型文件

| 文件 | 说明 |
|---|---|
| `index.html` | 评审标注版方案一（移除开户行输入框） |
| `js/app.js` | 评审标注版逻辑（移除开户行校验） |
| `index-original.html` | 纯净版方案一（移除开户行输入框） |
| `js/app-original.js` | 纯净版逻辑（移除开户行校验） |

> 方案二相关文件（`v2.html`、`js/app-v2.js`、`v2-original.html`、`js/app-v2-original.js`）不受影响，保持原样。

## 7. 文档同步

| 文档 | 变更 |
|---|---|
| `PRD.md` | 方案一流程/字段/接口去除开户行；方案二描述保持开户行；版本记录新增 v1.3 |
| `sequence-diagram.md` | 基于方案二，未改动 |
