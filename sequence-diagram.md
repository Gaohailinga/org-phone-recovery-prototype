# 一级机构手机号找回方案时序图

> 基于推荐方案二：点击「我是一级机构」→ 结算账户信息验证 → 换绑手机号 → 换绑成功 → 返回找回密码页。

```mermaid
sequenceDiagram
    autonumber
    actor U as 用户/法人
    participant FE as 前端页面
    participant AS as 机构档案

    rect rgb(255, 251, 200)
        Note over U,AS: 阶段一：一级机构入口
        U->>FE: 在找回密码页点击「我是一级机构」
        FE->>FE: 跳转结算账户信息验证页
    end

    rect rgb(255, 240, 220)
        Note over U,AS: 阶段二：结算账户信息验证
        U->>FE: 输入机构ID、开户名称、开户账号
        U->>FE: 点击下一步
        FE->>AS: 校验结算账户信息
        AS-->>FE: 返回校验结果
        alt 校验通过
            FE-->>U: 跳转换绑手机号页
        else 校验失败
            FE-->>U: 提示校验失败
        end
    end

    rect rgb(232, 245, 233)
        Note over U,AS: 阶段三：换绑手机号
        U->>FE: 输入新手机号并获取验证码
        U->>FE: 输入验证码并点击确认
        FE->>AS: 更新绑定手机号
        AS-->>FE: 返回更新结果
        alt 更新成功
            FE-->>U: 展示「换绑成功」
            U->>FE: 点击「继续找回密码」
            FE->>FE: 将新手机号带入找回密码页
            FE-->>U: 返回找回密码页
        else 更新失败
            FE-->>U: 提示失败原因
        end
    end
```

## 说明
- 流程与 `v2.html` 页面结构保持一致：找回密码页 → 结算账户信息验证页 → 换绑手机号页 → 换绑成功页 → 返回找回密码页。
- 校验或更新失败时停留在当前页，用户可修正后重新提交。
- 换绑成功后点击「继续找回密码」，前端自动将新手机号回填至找回密码页的手机号输入框，形成完整闭环。
