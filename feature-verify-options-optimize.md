# 验证方式选择页文案优化 + 法人验证图形验证码方案

| 项目 | 内容 |
|---|---|
| 版本 | v1.4 |
| 更新日期 | 2026-09-02 |
| 状态 | 已完成（原型已更新并上线） |
| 作者 | 产品经理 |
| 所属需求 | 一级机构手机号找回方案（PRD v1.4） |

---

## 1. 背景

方案一（双验证方式）原型上线后，经评审反馈两点优化：
1. 验证方式选择页卡片说明文案过长、信息密度低，需要精简；
2. 法人信息验证页「获取验证码」未做防刷策略，需增加图形验证码先验，策略与 CSP 手机号验证码登录保持一致。

## 2. 方案目标

- 精简验证方式选择页卡片说明文案，提升可读性；
- 法人信息验证增加**图形验证码**：点击「获取验证码」时**先校验图形验证码**，验证通过后才发送短信验证码；
- 图形验证码策略与 **CSP 手机号验证码登录**保持一致（样式、校验、失败提示、自动刷新）。

## 3. 变更内容

### 3.1 验证方式选择页文案优化

| 卡片 | 变更前 | 变更后 |
|---|---|---|
| 法人信息验证（说明） | 通过机构ID、法人姓名、入驻时使用的法人身份证号码及法人手机号进行身份核验 | **机构ID + 法人姓名 + 法人身份证号 + 法人手机号** |
| 法人信息验证（提示） | 若入驻时使用的是非法人身份证，请选择结算账户信息验证 | **若入驻时使用非法人身份证，请选结算账户验证** |
| 结算账户信息验证（说明） | 通过机构ID、开户名称及开户账号进行身份验证 | **机构ID + 开户名称 + 开户账号** |

> 说明：长句精简为字段清单（信息等价），提示保留关键引导（非法人身份证场景引导至结算账户验证）。

### 3.2 法人信息验证增加图形验证码

**页面结构变更**

```
法人信息验证页（变更后表单）
  ├─ 机构ID          （必填）
  ├─ 法人姓名        （必填）
  ├─ 法人身份证号    （必填，18 位）
  ├─ 法人手机号      （必填，11 位）
  ├─ 图片验证码      （必填，4 位，新增）+ 验证码图形框（可点击刷新）
  └─ 短信验证码      （必填，4 位）
```

**交互流程（与 CSP 手机号验证码登录一致）**

```
用户点击「获取验证码」
  ├─ 法人手机号为空/非法 → 提示「请先输入正确的法人手机号」
  ├─ 图片验证码为空/错误 → 提示「验证码不正确」，自动刷新图形验证码
  └─ 图片验证码正确 → 发送短信验证码，按钮进入 60s 倒计时
```

**图形验证码样式（与 CSP 登录保持一致）**
| 项目 | 内容 |
|---|---|
| 输入框 | 与短信验证码一致的输入框（`form-item-with-aside` 布局） |
| 验证码图形框 | `captcha-box` 样式（红橙蓝渐变背景 + 白色粗体数字） |
| 刷新方式 | 点击图形框自动切换验证码 |
| 校验方式 | 不区分大小写比对（输入转大写后比对） |
| 校验失败 | 提示「验证码不正确」并自动刷新图形验证码 |

**其他校验**
- 点击「下一步」时增加图片验证码校验（错误提示「请输入正确的图片验证码」）；
- 图形验证码错误即刷新，防止暴力重试。

## 4. 接口变更

无接口字段变更。短信验证码下发接口需在服务端先校验图形验证码：

```
POST /api/org/send-legal-sms-code
```

**请求参数**
| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| legalPhone | string | 是 | 法人手机号 |
| imgCode | string | 是 | 图形验证码（先验） |

**返回示例**
```json
{
  "code": 0,
  "data": { "sent": true }
}
```

## 5. 涉及原型文件

| 文件 | 说明 |
|---|---|
| `index.html` | 评审标注版方案一（文案精简 + 法人验证页新增图片验证码输入框与图形框） |
| `js/app.js` | 评审标注版逻辑（`refreshLegalCaptcha` 刷新、`checkCaptcha` 先验、getcode/next 校验） |
| `index-original.html` | 纯净版方案一（同步文案与图片验证码） |
| `js/app-original.js` | 纯净版逻辑（同步） |

> 方案二相关文件（`v2.html`、`js/app-v2.js`、`v2-original.html`、`js/app-v2-original.js`）不受影响。

## 6. 文档同步

| 文档 | 变更 |
|---|---|
| `PRD.md` | 待更新：验证方式选择页文案、法人验证页图片验证码策略、字段规则、接口、版本记录 v1.4 |
